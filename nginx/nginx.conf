events {}

http {

    server {
        listen 80;

        location / {
            proxy_pass http://172.31.1.111:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

}
