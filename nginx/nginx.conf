events {}

http {

    server {
        listen 100;

        location / {
            proxy_pass http://springboot-app:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

}