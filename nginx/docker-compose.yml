version: "3.8"

services:

  nginx:
    image: nginx:1.25-alpine
    container_name: productapp-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: always
